# VPS deployment only — do not run docker compose on your local machine.
# Container: edquate-standalone-frontend | Host port: 3202 (Ravan frontend uses 3201)
API_BACKEND_URL=https://v2.edquate.com:8443
NEXT_PUBLIC_API_BASE=/api/v2
